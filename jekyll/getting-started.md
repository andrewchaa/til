# install jekyll

```
brew install ruby
gem update --system
gem install jekyll
gem install bundler
bundler install
```

# create a site

```
jekyll new my-site
```

# start jekyll server

```
jekyll serve
```

# build a static site

```
jekyll build
```
