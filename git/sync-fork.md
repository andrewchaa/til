

```
// Add a new remote, upstream
git remote add upstream git@github.je-labs.com:cogman/app_RestaurantManagement.git
git remote -v

// merge upstream/master to the local master
git merge upstream/master
```

